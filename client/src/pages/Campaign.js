import React from 'react';

const Campaign = () => {
  return (
    // Stand-in CSS
    <div className="container">
      {/* Component for campaign list, characters list */}
    </div>
  );
};